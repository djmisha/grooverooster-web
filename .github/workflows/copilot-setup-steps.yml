name: "Copilot Setup Steps"

# Automatically run the setup steps when they are changed to allow for easy validation, and
# allow manual testing through the repository's "Actions" tab
on:
  workflow_dispatch:
  push:
    paths:
      - .github/workflows/copilot-setup-steps.yml
  pull_request:
    paths:
      - .github/workflows/copilot-setup-steps.yml

jobs:
  # The job MUST be called `copilot-setup-steps` or it will not be picked up by Copilot.
  copilot-setup-steps:
    runs-on: ubuntu-latest

    # Set the permissions to the lowest permissions possible needed for your steps.
    # Copilot will be given its own token for its operations.
    permissions:
      # If you want to clone the repository as part of your setup steps, for example to install dependencies,
      # you'll need the `contents: read` permission. If you don't clone the repository in your setup steps,
      # Copilot will do this for you automatically after the steps complete.
      contents: read

    # You can define any steps you want, and they will run before the agent starts.
    # If you do not check out your code, Copilot will do this for you.
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      # TODO: Fix peer dependency conflict between date-fns@4.1.0 and react-day-picker@8.10.1
      # react-day-picker@8.10.1 requires date-fns@^2.28.0 || ^3.0.0, but we're using date-fns@4.1.0
      # Options: 1) Downgrade date-fns to v3.x, 2) Upgrade react-day-picker to a version that supports date-fns v4
      # For now, using --legacy-peer-deps to bypass the conflict
      - name: Install JavaScript dependencies
        run: npm ci --legacy-peer-deps

      - name: Build Next.js project
        run: npm run build
        env:
          # Add any required environment variables for the build
          NODE_ENV: production
